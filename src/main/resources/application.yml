spring:
  application:
    name: badminton-academy-management
  profiles:
    active: dev
  mail:
    host: smtp.gmail.com
    port: 587
    username: adhnanjeff26@gmail.com
    password: qtfo jaby sqgl okoj
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  jpa:
    open-in-view: false
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
  cache:
    type: caffeine
    cache-names:
      - students:all
      - students:active
      - students:byId
      - students:bySkillLevel
      - students:byParent
      - students:byBatch
      - students:byCoach
      - students:countBySkillLevel
      - students:feeHistory
    caffeine:
      spec: maximumSize=2000,expireAfterWrite=30m

server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always

jwt:
  secret: ${JWT_SECRET:your-secret-key-change-in-production-must-be-at-least-256-bits}
  expiration: 86400000
  refresh-expiration: 604800000

# OTP Configuration
otp:
  expiry-minutes: 5
  max-requests-per-hour: 5
  max-verify-attempts: 5

# SMS Provider Configuration
sms:
  provider: mock  # Change to 'twilio' or 'aws-sns' in production

twilio:
  account-sid: ${TWILIO_ACCOUNT_SID:}
  auth-token: ${TWILIO_AUTH_TOKEN:}
  from-number: ${TWILIO_FROM_NUMBER:}

stripe:
  api-key: ${STRIPE_API_KEY:your-stripe-key}

app:
  seed-data: false
  mail:
    from: ${MAIL_FROM:adhnanjeff26@gmail.com}
  cors:
    allowed-origins:
      - http://localhost:4200
      - http://127.0.0.1:4200
      - http://localhost:3000

# Attendance Configuration
attendance:
  backdate:
    coach-window-days: 7  # Coaches can backdate attendance up to 7 days
    admin-window-days: 30 # Admins can backdate attendance up to 30 days
